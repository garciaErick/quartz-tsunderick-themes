# child-site .gitignore — copy to your repo root as .gitignore:
#   cp engine/templates/gitignore .gitignore
#
# Everything the engine extracts is regenerated by engine/scripts/build.sh
# on every build — never track it. You track only: content/, quartz.config.yaml,
# static/, styles/, site-plugins/, engine/ (submodule pointer), .node-version.

# build output & caches
.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
.quartz/
.turbo/
private/
.engine-deps-stamp

# engine-extracted by engine/scripts/build.sh (regenerated every build)
quartz/
plugins/
quartz.ts
quartz.config.default.yaml
package.json
package-lock.json
tsconfig.json
globals.d.ts
index.d.ts
.npmrc
.prettierignore
.prettierrc
